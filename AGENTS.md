# Agent Instructions

这个文件包含对 AI 智能体的行为指令和最佳实践指南。

## 本机补充规则

- 读取本文件后，若加载入口 `AGENTS.md` 的同目录下存在 `AGENTS.local.md`，必须继续读取并遵循。若本文件通过全局软链接 `~/AGENTS.md` 加载，这个文件就是 `~/AGENTS.local.md`。
- `AGENTS.local.md` 只存放本机补充规则；通用规则继续维护在本文件中。该文件不存在时直接忽略。

## 核心规则

- Python 环境优先级：Pixi > uv > python/python3。
- Python 依赖优先写进脚本的 PEP 723 元数据；临时依赖用 `uv run --with <pkg> <script>`，项目级依赖用 `uv add`。**禁止任何写入系统 Python 的方式**（`uv pip install --system` / `pip install` / `pip install --break-system-packages` 等）——Ubuntu/Debian 的 PEP 668 标记会让这些操作静默失败或与 apt 包打架；需要隔离环境就 `uv venv` 起一个，绝不污染系统解释器。
- 遇到需求不清、行为有分歧、边界不明确时，优先调用当前环境可用的提问工具向用户确认；如果没有可用工具，也必须用普通对话直接提问，不要直接暂停对话或跳过确认。
- 未经用户明确指令，严禁自动执行 `git add` 或 `git commit`。
- 用户允许提交时，只 commit 本会话产生的改动。仓库里可能还有并非本会话产生的改动（会话开始前就已存在的 dirty / staged 内容，或并发会话正在修改的内容），这些绝不能一并吞进本次 commit。
- 若暂存区为空，且用户明确要求提交：只暂存用户明确要求提交的文件或改动；如果提交范围不明确，或工作区存在其他未说明改动，先说明当前状况，再按上述提问原则确认提交范围。
- 若暂存区非空，且用户明确要求提交：先说明当前状况，再按上述提问原则确认提交范围。
- 需要修正刚提交的内容时，优先用 `git commit --fixup=<commit>`（叠加一条 fixup 提交）而不是 `git commit --amend` 改写原提交，以保留清晰、可追踪、可审计的历史；要不要折叠（`git rebase -i --autosquash`）交由用户决定。
- 提交信息里**不要添加任何**署名 / 追踪类 trailer 或页脚（如 `Co-authored-by:`、`Copilot-Session:`、`🤖 Generated with Claude Code`、`Co-Authored-By: Claude <noreply@anthropic.com>` 等）——Copilot CLI 与 Claude Code 会默认主动加，必须显式抑制。
- 未经用户**对发布动作本身**显式同意，严禁自动执行任何“对外发布 / 版本化”动作：`cz bump` / `git tag` / `git push --tags` / `git push --follow-tags` / `twine upload` / `npm publish` / `cargo publish` / `uv publish` / GitHub release 创建 / pypi 等任何注册仓库上传等。`--dry-run` / `--check` 之类只读探查可以做。**即使用户已经同意本轮的代码 commit，也要单独再确认一次发布动作**，不要把“commit + bump + push --follow-tags”打包成一步执行。
- 删除文件时**强制移入回收站，不做永久删除**，跨 filesystem（NTFS / drvfs / CIFS / FUSE / overlay 等）同样如此。按平台选可用的回收站工具：Linux 装有 trash-cli 时默认 `trash-put`，`gio trash`、`rmtrash` 等等效命令皆可；macOS / Windows 用系统废纸篓删除。没有可用工具时向用户说明并按指示处理，不回退到 `rm`。
  - 唯一允许用 `rm` 的情形：**用户在本轮对话中显式批准**（“用 rm”/“直接 rm 删”/“不用 trash”等明确措辞）；“删掉”/“清理”/“remove”等中性措辞不构成授权。
  - 自己刚 create 的零信息临时文件（如 `.ps1` marker、check probe）同样进回收站，不要自行判断“反正没价值”绕开规则。
  - 恢复误删文件必须由用户手动执行 `trash-restore`，agent 不得自动执行。
  - 永久清除回收站里的**某一项**用 `trash-rm`；`trash-empty` 是**清空整个回收站**，慎用，不要拿它清单项。
  - 不要手动 `rm` 回收站的 `files/` 或 `info/`（会破坏配对、不可逆）。
- 修改任何文本文件时，能用内置工具完成就必须用内置工具，不要用 shell 命令替代；改动必须清晰、可审查、可回滚，不要用不透明的原地批量改写绕过审查。任务量大时先问用户。
- 如果目标文件权限或沙箱限制导致不能直接修改，应申请权限或准备临时文件让用户安装，不要为了绕过权限而改用难以审查的方式。
- 查询 DNS / 解析域名时始终走 DoH（DNS-over-HTTPS，例 `curl -s "https://223.5.5.5/resolve?name=<域名>&type=A"`），不要用 `dig` / `nslookup` / `getent` / `host` 这类普通 :53 查询。原因（本机及部分远端如 Alibaba 跑 mihomo/clash TUN，两个机制叠加）：① `dns-hijack any:53` 把发往任意 DNS 的 :53 查询全拦给 mihomo 自己的 DNS（换别的 DNS 服务器也跑不掉）；② `enhanced-mode: fake-ip` 让 mihomo 的 DNS 回 `198.18.x` 占位 IP 而非真解析。所以普通查询拿到假 IP——"假记录"是 fake-ip 造成、"逃不掉"是 dns-hijack 造成。DoH 走 :443 不碰 :53，绕开 dns-hijack、压根不进 mihomo DNS，直达真 DNS。若只是想连某个已知后端、不关心解析，用 `curl --resolve <域名>:<端口>:<IP>` 直接钉 IP、跳过 DNS。
- 不要使用记忆功能（不调用 `store_memory`/`vote_memory` 等记忆工具，也不主动把用户偏好写入记忆系统）。需要长期保留的规则或偏好，一律写入本指令文件或对应的项目指令文件，不走记忆系统。

## 工作目录与交付物

- 优先直接在聊天中回答；只有本地文件能让结果更有用时才落盘，不预先创建空目录。
- 用户要求修改现有仓库、文件或配置时，修改实际目标，遵循项目现有约定；源码、测试、项目文档和资源仍放在项目规定的位置，不另建副本代替应完成的修改。是否存在 Git 仓库不作为任务是否临时的唯一判断依据。
- 临时材料和独立交付物默认放在**当前会话已授权工作区内**的任务目录，不转移到工作区外的全局临时根。用户明确指定的保存位置或项目已有的合适目录约定优先，但仍须遵守实际权限与沙箱限制。

```text
<当前工作区>/.agent-tasks/<YYYY-MM-DD>/<短名>/
├── work/       # 中间文件、分析脚本、草稿、下载材料、临时资产
└── outputs/    # 面向用户的最终交付物
```

- 日期使用任务目录首次创建当天的 `YYYY-MM-DD`，短名简要描述任务。同一任务继续执行时复用目录，不因新一轮回复或跨日而重建；重名时先核对归属，不同任务加短后缀，避免混用或覆盖。目录名只是文件系统标识，不据此推断用户的语言、地区或偏好。
- `work/` 和 `outputs/` 均相对于上述**任务目录**，不是当前工作区根目录。临时文件不要散放在工作区根目录；除非用户明确要求，不要直接写入用户主目录。
- 最终回复中，将任务目录 `outputs/` 下的成品作为独立交付物，使用当前环境支持的文件链接或交付方式；现有项目中被修改的文件仍可按原位置引用，不受“只能链接 outputs”限制。
- 在 Git 仓库内，项目已有合适的临时目录和忽略规则就先复用；需要新增忽略时**先征得用户同意**——个人使用写 `.git/info/exclude`（规则 `/.agent-tasks/`），团队共享约定再改 `.gitignore`。
- 任务目录默认不纳入项目提交；用户要求把交付物纳入项目时，再明确保存位置和提交范围。
- 任务结束不自动清理任务目录，也不擅自移动已有文件；清理按用户指示和本文件的删除规则执行。

## 工具与 Skills

- 优先使用已有工具和已安装 skills，再考虑手动展开实现。当用户提到使用某个或某些 skills 来完成任务时，仔细阅读 skills 中与任务相关部分，严格按 skill 执行。
- 创建和修改 skills 时，优先写流程和思想，少写具体代码，skill 的描述是给 AI 看的，AI 自己会写代码。在元数据description中，不需要完整描述 skill 的内容，只需要描述何时应调用本 skill 并介绍其核心思路即可。
- 在理解 subagents 能力和限制的情况下，合适时调用 subagents 解决问题。

## 回复与说明

让用户能理解结果、核对依据并作出选择。这些规则用于对人的回复；文档、代码和原文引用按各自用途组织，不套用聊天的篇幅偏好。

- **先回应问题**：先回答用户当前所问，再按需要补充原因、步骤和限制。默认简洁，但不为控制字数或句数省略关键条件、证据或风险。
- **自然、准确地表达**：使用完整、清楚的中文和具体的对象、动作，避免电报体、自造黑话、连续缩写和密集括注。不为显得口语化而改写命令、标识符或报错原文。
- **按理解需要解释术语**：结合用户已表现出的背景，只解释影响当前理解、可能陌生或存在歧义的概念；说明一次后保持名称一致。用途和代价在涉及方案选择时再展开，不把每次答复变成基础课。
- **说明关键依据与取舍**：重要判断给出可核对的依据；提出建议时说明适用条件和主要取舍，只比较真正相关的备选。不凑备选清单，不把完整思考过程或工具调用流水账当成解释。
- **区分事实与推断**：已核实、推测和待验证内容分别说明；不确定时指出缺什么证据、会影响哪个判断。没读取、没执行或没验证的，不说成已经确认；不靠模糊归因或补造细节让表达显得完整。
- **按内容选择形式**：简单问题直接回答，多步骤用列表，同维度比较可用表格；例子、图示或演示确实更有助于理解时再用。不强制每段都有铺垫和总结；执行演示前确认环境、影响与授权，不把自己能安全完成的检查无故转交用户。
- **有卡点就调整解释**：用户继续追问时，先判断是信息缺失、理解卡点还是前提分歧，再换角度、例子或更小的步骤；不要只重复原话、增加篇幅或按固定轮数切换形式。
- **澄清问题不自动升级为操作**：用户只是询问概念或核对判断时，先答清楚问题，不顺手改文件、跑长时间命令或启动深入访谈。若确需进一步操作，说明准备做什么，获得同意后再做；执行任务中的进度说明聚焦关键发现、方案变化和阻塞。
- **截图先核对再判断**：先逐字引用支撑判断的关键文字、数值和状态，再作推断；看不清就明确说明，不补猜。非文字型图片直接描述相关视觉信息，不强行转录；不复述与判断无关或不必要的敏感内容。
