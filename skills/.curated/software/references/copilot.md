# GitHub Copilot CLI 笔记已迁移

Copilot CLI / SDK / MCP / 会话导出，以及更宽的 agent harness 架构笔记现在由 `harness` skill 维护。

`software` 继续覆盖相邻的本地软件运维主题，例如 SSH、Zellij、systemd 服务、Git 镜像、对象存储客户端、Windows/WSL、macOS 与文档格式转换工具。
