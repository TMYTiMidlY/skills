# ICP 备案与端口策略

> 经验性总结，不是法律意见。内容融合了自有的大陆 ECS / 家宽实测（如 2026-04 阿里云 ECS 的 SNI 封锁实测）和 2026-05 与同好交流补充的备案豁免边界；具体合规边界以监管和运营商最新口径为准，长期对外服务请以备案/合规为目标。

国内自托管经常被同一个问题卡住："这个东西要不要备案？" 这份 reference 把常见组合归类，便于快速判断；以及由这些约束衍生出的部署模式。

## 速查矩阵

| 场景 | 是否需要备案 | 关键约束 |
|---|---|---|
| 海外 VPS + 自有域名 | 不需要 | 监管管不到海外资源 |
| 国内云 ECS + 自有域名 + **80/443** | **需要**，否则未备案域名走 80/443 会被 SNI 封锁 | 详见下文"国内云 ECS 上的 SNI 封锁实测" |
| 国内云 ECS + 纯 IP（不带域名） | 不需要 | 浏览器纯 IP 访问不发 SNI；用 `tls internal` + 客户端装 root CA |
| 国内云 ECS + 自有域名 + **非标端口** | 惯例上不需要 | 备案/封锁针对 80/443，非标端口基本不在主动拦截路径上 |
| **家宽公网 IP** + 自有域名 + **非标端口** | 不需要 | 域名只是解析到自己 IP；运营商通常封 80/443，所以本来也只能走非标端口 |
| 家宽公网 IP + 80/443 | —— | 大多数家宽运营商**直接封 80/443 入站**，技术上跑不起来 |

> "惯例上不需要"指 V2EX / 自建圈普遍口径与个人实践，不代表监管规则。规模一大就建议老老实实备案。

## 国内云 ECS 上的 SNI 封锁实测

2026-04 在阿里云大陆 ECS 上的实测观察：

- HTTPS 封锁看的是 TLS ClientHello 里的 SNI（明文），即目标域名。只要请求里带了未备案的域名，就会被拦。
- 最初用 `https://<域名>:8443` 访问是正常的；经过多次测试之后，家宽访问同一地址返回了阿里云"要求备案"的提示页，手机流量访问仍然正常。是不是反复测试导致命中的不确定，反正结果是家宽这边被拦了。

### 当前可用方案：IP 直连 + Caddy internal CA

结论：只要请求里不带域名就没事。浏览器用纯 IP 访问时默认不发 SNI，也没有域名可匹配，实测稳定可用，是目前在用的方案。

一个服务一个端口，`https://<主 IP>` 走默认的 443 也能正常访问；其他服务用各自的端口。TLS 统一 `tls internal` 自签，客户端装 Caddy local root CA，见 [caddy.md](caddy.md) 的"导入 Caddy local root CA（仅 `tls internal` 场景）"小节。

> 严格讲这是违规操作，V2EX 上有反馈过"一个星期后接到电话"的案例，共识是小规模用通常被容忍。长期要合规还是得备案或走海外前端反代。

## 家宽公网 IP + 非标端口 自用 HTTPS

只要满足以下三点，自用 HTTPS 服务在国内家宽公网 IP 上是常规做法：

1. **走非标端口**（如 `:10003`、`:8443`），绕开运营商对 80/443 的入站封锁。
2. **域名解析到自己的 IP**（A / AAAA 记录），不经过国内云厂商也不在国内 CDN 上。
3. **自用规模**，不对公众宣传/推广。

证书直接走 Let's Encrypt 就行——非 80/443 上的 HTTPS 不在主动 SNI 拦截路径上，不影响签发也不影响访问。范例形式：

```
https://ddns.shenjack.top:10003/dashboard
```

跟"国内 ECS 上未备案域名 + 非标端口"是同一类豁免——拦截的是 SNI 走 80/443 的明文请求，不是所有 HTTPS。

> 补充："**有域名 + 非标端口正常签 Let's Encrypt**" 跟 [caddy.md](caddy.md) 的"IP 模式（无域名 / 未备案）"是互补关系：caddy.md 教的是"没有域名时用 `tls internal` + 客户端装 Caddy local root CA 凑合"；这里讲的是"有域名时用真实 CA 签发的非标端口 HTTPS"。两条路按是否有域名 + 是否能接受导入 root CA 来选。

## 由备案约束衍生的部署模式

家宽公网 IP 在国内**几乎必然**遇到两个限制：

- 80/443 入站被运营商封；
- NAT/CGNAT 让某些设备根本拿不到真公网 IP。

为此常见的妥协做法是**额外买一台小的国内 VPS 做反代/组网中转**：

- 国内 VPS 拿一个公网 IP，跑 EasyTier / frp / WireGuard，把家里设备拉进虚拟网络。
- 对外暴露的 HTTPS 在国内 VPS 上用非标端口落地（同样适用上面的备案豁免规则），后端反代到家里设备。
- 国内 VPS 的好处是延迟低、对国内访问友好；缺点是受备案/审计约束更强，所以**只跑组网中转和反代落地，不做面向公众的服务**。

EasyTier 的具体配置见 [easytier.md](easytier.md)；Caddy 反代见 [caddy.md](caddy.md)。

## 关键词索引

- 关键词：`备案`、`ICP`、`非标端口`、`公网 IP`、`家宽`、`80 443`、`SNI 封锁`、`未备案`、`内网穿透`、`组网中转`、`Let's Encrypt 非标端口`、`域名解析自家 IP`、`阿里云 ECS 要求备案 提示页`、`tls internal 大陆`、`IP 直连 SNI`
